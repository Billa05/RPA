<Activity mc:Ignorable="sap sap2010" x:Class="CertifyHub.SeedDemoData"
  xmlns="http://schemas.microsoft.com/netfx/2009/xaml/activities"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:sap="http://schemas.microsoft.com/netfx/2009/xaml/activities/presentation"
  xmlns:sap2010="http://schemas.microsoft.com/netfx/2010/xaml/activities/presentation"
  xmlns:ui="http://schemas.uipath.com/workflow/activities"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <x:Members>
    <x:Property Name="in_ApiBaseURL" Type="InArgument(x:String)" />
  </x:Members>

  <Sequence DisplayName="Seed Demo Events and Attendees">
    <Sequence.Variables>
      <Variable x:TypeArguments="x:String" Name="SeedResponse" />
    </Sequence.Variables>

    <ui:LogMessage Level="Info" Message="&quot;Step 2: Seeding demo data…&quot;" />

    <TryCatch DisplayName="POST /api/seed">
      <TryCatch.Try>
        <Sequence>
          <ui:HttpClient DisplayName="Call Seed Endpoint"
            RequestUrl="[in_ApiBaseURL.ToString() + &quot;/seed&quot;]"
            Method="POST"
            Body="&quot;{}&quot;"
            BodyFormat="application/json"
            AcceptFormat="JSON"
            Result="[SeedResponse]"
            Timeout="10000" />
          <ui:LogMessage Level="Info" Message="[&quot;✓ Seed response: &quot; + SeedResponse]" />
        </Sequence>
      </TryCatch.Try>
      <TryCatch.Catches>
        <Catch x:TypeArguments="s:Exception" xmlns:s="clr-namespace:System;assembly=mscorlib">
          <ActivityAction x:TypeArguments="s:Exception">
            <ActivityAction.Argument>
              <DelegateInArgument x:TypeArguments="s:Exception" Name="ex" />
            </ActivityAction.Argument>
            <ui:LogMessage Level="Warn" Message="[&quot;⚠ Seed skipped (may already exist): &quot; + ex.Message]" />
          </ActivityAction>
        </Catch>
      </TryCatch.Catches>
    </TryCatch>

  </Sequence>
</Activity>
